# Core dependencies
fastapi==0.116.2  # Updated to support Starlette 0.48.0
hypercorn==0.17.3  # Updated to support h11>=0.16.0 for security fixes
python-dotenv==1.0.0
python-multipart>=0.0.20  # Minimum version for multipart form security
httpx==0.27.2  # Updated to support h11>=0.16.0 for security fixes
starlette==0.48.0  # Updated to address security vulnerability
urllib3>=2.5.0  # Explicitly pinned to address security vulnerability
h11>=0.16.0  # Explicitly pinned to address CVE-2024-62803 (request smuggling vulnerability)
requests>=2.32.5  # Explicitly pinned to address CVE-2024-35195 (.netrc credential leak vulnerability)

# OpenAI and YouTube
openai==1.68.2
youtube_transcript_api==0.6.2
pytube==15.0.0  # For extracting YouTube metadata without API key
tiktoken>=0.5.0  # Token counting for context window management

# Database
pymongo==4.8.0
psycopg[binary]==3.2.9
python-tds>=1.16.0  # SQL Server support (pure Python)

# AWS
boto3>=1.35.0  # Updated to Python 3.10+ version for urllib3 2.x support
botocore>=1.35.0  # Updated to Python 3.10+ version for urllib3 2.x support

# Datadog - updated to use pre-compiled wheels
ddtrace>=3.8.0  # Updated to use newer version with pre-compiled wheels

# Datadog (new additions)
datadog-api-client==2.20.0  # Add API client

# Kafka
confluent-kafka>=2.5.0

# Sentry - error tracking, tracing, and profiling
sentry-sdk[fastapi]>=2.44.0  # Includes FastAPI, Starlette, HTTPX, log+profiling integrations

# Email
# sendgrid==6.10.0  # SendGrid SDK for email notifications - REMOVED: Trial ended, migrating to Amazon SES

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# New additions
notion-client>=2.0.0
google-api-python-client
google-auth-oauthlib
google-auth-httplib2
